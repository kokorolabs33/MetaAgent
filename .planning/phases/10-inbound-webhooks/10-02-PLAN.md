---
phase: 10-inbound-webhooks
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - web/lib/types.ts
  - web/lib/api.ts
  - web/app/settings/webhooks/page.tsx
autonomous: false
requirements: [HOOK-03]

must_haves:
  truths:
    - "User can see a list of inbound webhook configurations on the Inbound tab"
    - "User can create a new inbound webhook and sees the generated URL and secret (shown once)"
    - "User can copy the webhook URL and secret to clipboard"
    - "User can toggle inbound webhooks active/inactive"
    - "User can delete inbound webhooks"
    - "User sees provider-specific setup instructions (GitHub webhook URL + content type, Slack slash command URL)"
    - "Outbound webhooks tab still works as before"
  artifacts:
    - path: "web/lib/types.ts"
      provides: "InboundWebhook TypeScript interface"
      contains: "interface InboundWebhook"
    - path: "web/lib/api.ts"
      provides: "API client methods for inbound webhook CRUD"
      contains: "inboundWebhooks"
    - path: "web/app/settings/webhooks/page.tsx"
      provides: "Tabbed UI with Outbound and Inbound tabs, full CRUD for inbound webhooks"
      min_lines: 150
  key_links:
    - from: "web/app/settings/webhooks/page.tsx"
      to: "/api/inbound-webhooks"
      via: "api.inboundWebhooks.* calls"
      pattern: "api\\.inboundWebhooks"
    - from: "web/lib/api.ts"
      to: "web/lib/types.ts"
      via: "InboundWebhook type import"
      pattern: "InboundWebhook"
---

<objective>
Build the frontend webhook management UI that lets users create, view, edit, and delete inbound webhook configurations. Add an "Inbound" tab to the existing webhooks settings page, with provider selection, auto-generated endpoint URLs, copy-to-clipboard for URLs and secrets, and provider-specific setup instructions.

Purpose: Users need a self-service way to configure inbound webhooks -- creating webhook endpoints, getting the URL and secret to paste into GitHub/Slack settings, and managing active/inactive state.

Output: Working inbound webhook management page with full CRUD, tabbed layout (Outbound/Inbound), and provider-specific guidance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-inbound-webhooks/10-CONTEXT.md
@.planning/phases/10-inbound-webhooks/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: Backend API contracts -->

CRUD endpoints (behind auth):
- GET /api/inbound-webhooks -> InboundWebhook[] (secrets masked as "***")
- POST /api/inbound-webhooks { name, provider } -> InboundWebhook (secret shown once, unmasked)
- GET /api/inbound-webhooks/{id} -> InboundWebhook (secrets masked)
- PUT /api/inbound-webhooks/{id} { name?, is_active?, rotate_secret? } -> InboundWebhook
- DELETE /api/inbound-webhooks/{id} -> 204

Ingestion endpoint URL pattern: /api/webhooks/inbound/{webhook.id}

Backend InboundWebhook JSON shape:
```json
{
  "id": "uuid",
  "name": "My GitHub Webhook",
  "provider": "github",
  "secret": "***",
  "previous_secret": "***",
  "is_active": true,
  "created_by": "user-uuid",
  "created_at": "2026-04-07T00:00:00Z"
}
```

From web/app/settings/webhooks/page.tsx (existing outbound webhooks page):
- Uses useState for webhooks, loading, showCreate, form fields
- Uses api.webhooks.list/create/update/delete
- Card-based list with toggle/delete buttons
- Full implementation is self-contained in single page component

From web/lib/api.ts (API client barrel pattern):
```typescript
export const api = {
  webhooks: { list, create, update, delete, test },
  // ... other namespaces
}
```

From web/lib/types.ts (type definition pattern):
```typescript
export interface WebhookConfig {
  id: string;
  name: string;
  url: string;
  events: string[];
  is_active: boolean;
  secret: string;
  created_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types, API client, and tabbed inbound webhook management UI</name>
  <read_first>
    - web/lib/types.ts (existing type patterns, WebhookConfig interface)
    - web/lib/api.ts (API client barrel pattern, existing webhooks namespace)
    - web/app/settings/webhooks/page.tsx (existing outbound webhooks page - full file)
    - web/components/ui/button.tsx (shadcn button for reference)
    - web/components/ui/input.tsx (shadcn input for reference)
  </read_first>
  <files>
    web/lib/types.ts,
    web/lib/api.ts,
    web/app/settings/webhooks/page.tsx
  </files>
  <action>
    1. Add InboundWebhook interface to `web/lib/types.ts` (after WebhookConfig):
    ```typescript
    export interface InboundWebhook {
      id: string;
      name: string;
      provider: string;       // "github" | "slack" | "generic"
      secret: string;         // "***" in list, full value on create
      previous_secret: string; // "***" in list
      is_active: boolean;
      created_by: string;
      created_at: string;
    }
    ```

    2. Add `inboundWebhooks` namespace to `web/lib/api.ts` (after the existing `webhooks` namespace):
    ```typescript
    inboundWebhooks: {
      list: () => get<InboundWebhook[]>("/api/inbound-webhooks"),
      get: (id: string) => get<InboundWebhook>(`/api/inbound-webhooks/${id}`),
      create: (data: { name: string; provider: string }) =>
        post<InboundWebhook>("/api/inbound-webhooks", data),
      update: (id: string, data: Partial<InboundWebhook> & { rotate_secret?: boolean }) =>
        put<InboundWebhook>(`/api/inbound-webhooks/${id}`, data),
      delete: (id: string) => del(`/api/inbound-webhooks/${id}`),
    },
    ```
    Import `InboundWebhook` type at the top of api.ts.

    3. Rewrite `web/app/settings/webhooks/page.tsx` to add tabbed layout per D-10:

    **Tab structure:**
    - Two tabs at the top: "Outbound" and "Inbound", styled as underlined text tabs (not heavy buttons).
    - Default tab: "Outbound" (preserves existing behavior for returning users).
    - Tabs use local state `activeTab: "outbound" | "inbound"`.

    **Outbound tab:**
    - Preserve ALL existing outbound webhook functionality exactly as-is. Move the existing page content into a component or conditional render block. Do NOT remove or modify any outbound functionality.

    **Inbound tab (per D-11, D-12):**

    **List view:**
    - Fetch inbound webhooks via `api.inboundWebhooks.list()` on tab activation.
    - Show webhook cards with: name, provider badge (color-coded: GitHub=gray-800, Slack=purple-900, Generic=blue-900), auto-generated endpoint URL (displayed as monospace text), status toggle (active/inactive), delete button.
    - The endpoint URL displayed is: `{window.location.origin}/api/webhooks/inbound/{webhook.id}`. Compute this client-side from the webhook ID.
    - Empty state: "No inbound webhooks configured. Create one to receive events from external services."

    **Create form (shown when "Create Inbound Webhook" button clicked):**
    - Name input (text)
    - Provider select: dropdown or radio buttons for "GitHub", "Slack", "Generic"
    - On submit: call `api.inboundWebhooks.create({ name, provider })`
    - **Secret reveal (per D-12):** After successful creation, show a prominent card with:
      - The webhook endpoint URL with a copy button (uses `navigator.clipboard.writeText`)
      - The generated secret with a copy button
      - Warning text: "Save this secret now. It will not be shown again."
      - Provider-specific setup instructions:
        - GitHub: "Go to your repository Settings > Webhooks > Add webhook. Paste the URL above as the Payload URL. Set Content type to application/json. Paste the secret above. Select 'Push events' and/or 'Pull requests' for events."
        - Slack: "Go to api.slack.com > Your App > Slash Commands > Create New Command. Paste the URL above as the Request URL. Or use Event Subscriptions and paste the URL as the Request URL."
        - Generic: "Send POST requests to the URL above with JSON body {\"title\": \"...\", \"description\": \"...\"}. Include the HMAC-SHA256 signature in the X-Webhook-Signature header."
      - "Done" button that dismisses the reveal card and refreshes the list.

    **Toggle active/inactive:** Call `api.inboundWebhooks.update(id, { is_active: !current })`. Update local state optimistically.

    **Delete:** Call `api.inboundWebhooks.delete(id)`. Remove from local state.

    **Rotate secret (nice to have, Claude's discretion):** Small "Rotate Secret" button on each card. Calls `api.inboundWebhooks.update(id, { rotate_secret: true })`. Shows new secret in a reveal card (same pattern as create).

    **Styling:** Follow existing page patterns -- same card borders, text colors, button styles as the outbound tab. Use shadcn/ui Button and Input components. Use Tailwind utility classes consistently.
  </action>
  <verify>
    <automated>cd /Users/jasper/Documents/code/kokorolabs33/taskhub && cd web && npx tsc --noEmit && npx next lint</automated>
  </verify>
  <acceptance_criteria>
    - InboundWebhook interface added to types.ts with all fields matching backend JSON
    - API client has inboundWebhooks namespace with list/get/create/update/delete methods
    - Webhooks settings page has two tabs: Outbound and Inbound
    - Outbound tab preserves all existing functionality without changes
    - Inbound tab shows webhook list with provider badges and endpoint URLs
    - Create flow generates webhook, shows secret once with copy buttons and setup instructions
    - Toggle and delete work
    - TypeScript compiles with no errors, ESLint passes
  </acceptance_criteria>
  <done>Inbound webhook management page fully functional with tabbed layout, CRUD operations, secret reveal, copy-to-clipboard, and provider-specific setup instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify inbound webhook flow end-to-end</name>
  <files>none</files>
  <action>
    Human verification of the complete inbound webhook system spanning both Plan 01 (backend) and Plan 02 (frontend). The user will test the full flow: create webhook via UI, copy credentials, send a test webhook via curl, verify task creation, test idempotency, and test inactive webhook behavior.
  </action>
  <verify>
    <automated>cd /Users/jasper/Documents/code/kokorolabs33/taskhub && go build ./cmd/server/ && cd web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Backend and frontend both compile without errors
    - Migration 008 runs on startup
    - Outbound tab unchanged
    - Inbound tab shows CRUD operations
    - Curl with valid HMAC creates a task
    - Duplicate delivery is rejected
    - Inactive webhook returns 404
  </acceptance_criteria>
  <done>User has verified end-to-end flow and approved.</done>
  <what-built>
    Complete inbound webhook system: backend ingestion with HMAC verification, GitHub/Slack/generic parsers, idempotency protection, and frontend management UI with tabbed layout and provider-specific instructions.
  </what-built>
  <how-to-verify>
    1. Start the backend: `make dev-backend` (verify migration 008 runs on startup in logs)
    2. Start the frontend: `make dev-frontend`
    3. Go to http://localhost:3000/settings/webhooks (or /manage/settings/webhooks)
    4. Verify the "Outbound" tab shows existing outbound webhooks (if any) -- confirm nothing is broken
    5. Click the "Inbound" tab
    6. Click "Create Inbound Webhook"
    7. Enter name "Test GitHub", select provider "GitHub", click Create
    8. Verify the secret reveal card shows:
       - Endpoint URL (copy it)
       - Secret value (copy it)
       - GitHub-specific setup instructions
       - Warning about saving the secret
    9. Click "Done" -- verify the webhook appears in the list with provider badge and URL
    10. Test the ingestion endpoint with curl (using the copied URL and secret):
        ```
        # Compute HMAC signature
        SECRET="<copied-secret>"
        BODY='{"ref":"refs/heads/main","repository":{"full_name":"test/repo"},"pusher":{"name":"testuser"},"compare":"https://github.com/test/repo/compare/abc...def","head_commit":{"message":"Test commit message"}}'
        SIG=$(echo -n "$BODY" | openssl dgst -sha256 -hmac "$SECRET" | awk '{print $2}')

        curl -X POST http://localhost:8080/api/webhooks/inbound/<webhook-id> \
          -H "Content-Type: application/json" \
          -H "X-Hub-Signature-256: sha256=$SIG" \
          -H "X-GitHub-Event: push" \
          -H "X-GitHub-Delivery: test-delivery-001" \
          -d "$BODY"
        ```
    11. Verify response is `{"status":"accepted","task_id":"<uuid>"}`
    12. Check the dashboard -- a new task should appear with title containing "test/repo" and "Test commit message"
    13. Send the same curl again (same X-GitHub-Delivery header) -- verify response is `{"status":"duplicate"}`
    14. Toggle the webhook inactive, send curl again -- verify 404 response
    15. Delete the webhook from the UI -- verify it disappears from the list
  </how-to-verify>
  <resume-signal>Type "approved" if the full flow works, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> CRUD API | Authenticated user manages webhook configs |
| UI -> Clipboard | Secret value copied to clipboard (transient) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information Disclosure | Secret in create response | mitigate | Secret shown only once on create; all subsequent List/Get responses mask as "***" |
| T-10-10 | Information Disclosure | Clipboard | accept | Standard browser clipboard API; user is responsible for not sharing secret; warning text displayed |
| T-10-11 | Spoofing | CRUD operations | mitigate | All CRUD endpoints behind RequireAuth middleware; only authenticated users can manage webhooks |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes (no TypeScript errors)
2. `npx next lint` passes (no ESLint errors)
3. Human verification of full end-to-end flow including curl test
</verification>

<success_criteria>
- Tabbed webhooks page with functional Outbound and Inbound tabs
- Inbound CRUD works: create shows secret once, list shows masked secrets, toggle and delete work
- Provider-specific setup instructions displayed on create
- Copy-to-clipboard works for URL and secret
- End-to-end: curl with valid HMAC creates a task, duplicate delivery is rejected, inactive webhook returns 404
</success_criteria>

<output>
After completion, create `.planning/phases/10-inbound-webhooks/10-02-SUMMARY.md`
</output>
