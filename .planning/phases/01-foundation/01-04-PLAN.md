---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - README.md
autonomous: false
requirements:
  - FOUND-04
must_haves:
  truths:
    - "README contains a hero screenshot or GIF showing the TaskHub UI with a working task"
    - "README has a badge row with build status, license, Go version, and Next.js version"
    - "README has an architecture diagram showing the orchestration flow"
    - "README quickstart section uses docker compose up as the primary method"
    - "README quickstart works on first attempt for a developer with only Docker and an API key"
  artifacts:
    - path: "README.md"
      provides: "Comprehensive GitHub-ready README"
      contains: "docker compose up"
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "Quickstart references docker compose up"
      pattern: "docker compose up"
---

<objective>
Rewrite the README to be GitHub-ready with hero screenshot, badge row, architecture diagram, and a quickstart that uses docker compose up as the primary path.

Purpose: FOUND-04 requires a comprehensive README that makes the repo credible on first visit. The current README has good content but the quickstart assumes manual setup (Go, Node, pnpm, createdb). The new quickstart should lead with Docker Compose (created in Plan 03) as the one-click path.

Output: Updated README.md with hero screenshot reference, badges, architecture diagram, and docker-compose-first quickstart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README with Docker-first quickstart, updated badges, and architecture diagram (per D-12)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content to preserve good sections)
    - docker-compose.yml (quickstart references this — read Plan 03 output)
    - CLAUDE.md (project conventions for reference)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12, D-13 requirements)
  </read_first>
  <action>
Rewrite `README.md` preserving the good existing content but restructuring for GitHub-ready presentation per D-12.

**Structure (in order):**

1. **Hero section** — centered title, tagline, link row, badge row
   - Keep existing title and tagline
   - Update badges: fix Go version to `1.26+` (per go.mod), keep license and Next.js badges
   - Add screenshot reference: `![TaskHub Dashboard](docs/images/hero-screenshot.png)` — the actual screenshot will be captured in the checkpoint task (D-13). For now use the existing `docs/images/task-dashboard.png` if it exists, otherwise use a placeholder path.

2. **One-paragraph description** — keep the existing "TaskHub is an open-source platform..." paragraph

3. **Quick Start (Docker)** — primary method, 3 steps:
   ```
   ## Quick Start

   ### Prerequisites
   - Docker and Docker Compose
   - An Anthropic API key ([get one here](https://console.anthropic.com/))

   ### 1. Clone
   ```bash
   git clone https://github.com/kokorolabs/taskhub.git
   cd taskhub
   ```

   ### 2. Start
   ```bash
   ANTHROPIC_API_KEY=sk-your-key docker compose up
   ```

   ### 3. Open
   Visit **http://localhost:3000** — no login required. Four demo agents are pre-registered.

   Create a task and watch the DAG executor decompose it, route to agents, and stream results in real-time.
   ```

4. **Development Setup** (collapsible) — for contributors who want to run without Docker:
   ```
   <details>
   <summary>Development Setup (without Docker)</summary>
   ... existing Prerequisites (Go, Node, pnpm, PostgreSQL) and make commands ...
   </details>
   ```

5. **Features** — keep existing features section but tighten

6. **Architecture** — keep existing ASCII diagram, it's good

7. **Agent Protocol** — keep existing section (Native + HTTP Poll adapter)

8. **Tech Stack** — keep existing table, update Go version to 1.26+

9. **Project Structure** — keep existing section

10. **Development commands** — `make check`, `make test`, etc.

11. **Roadmap** — update to reference the 5-phase plan:
    ```
    - [x] Agent Registry with HTTP poll + native adapters
    - [x] DAG-based task execution with parallel subtasks
    - [x] Real-time SSE streaming with event persistence
    - [x] Group Chat with @mention interaction
    - [ ] Agent status indicators (online/working/idle/offline)
    - [ ] Task templates with experience accumulation
    - [ ] Multi-task parallel dashboard
    - [ ] Sub-agent chat intervention
    ```

12. **Contributing** — keep existing section

13. **License** — keep existing

**Key changes from current README:**
- Docker Compose is the PRIMARY quickstart (not Go + Node + pnpm)
- Manual setup is in a collapsible `<details>` block
- Badge Go version updated to 1.26+
- Roadmap reflects actual 5-phase plan
- "Why TaskHub?" section preserved
- Remove "Community Edition" language — simplify to just "TaskHub"
- Update git clone URL to `kokorolabs/taskhub` (or keep as-is if the user has a different org)
  </action>
  <verify>
    <automated>cd /Users/jasper/Documents/code/kokorolabs33/taskhub && grep "docker compose up" README.md && grep "Quick Start" README.md && grep "badge" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "docker compose up" README.md returns a match (Docker-first quickstart)
    - grep "ANTHROPIC_API_KEY" README.md returns a match (API key in quickstart)
    - grep "localhost:3000" README.md returns a match (frontend URL)
    - grep "Architecture" README.md returns a match (architecture section exists)
    - grep "Quick Start" README.md returns a match (quickstart section exists)
    - grep "1.26" README.md returns a match (Go version badge updated)
    - grep "Development Setup" README.md returns a match (dev setup exists in collapsible)
  </acceptance_criteria>
  <done>README leads with Docker-first quickstart (3 steps), has updated badges, architecture diagram, and a clear path from clone to working demo. Manual dev setup is preserved in a collapsible section for contributors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify demo and capture screenshot (per D-13)</name>
  <files>docs/images/hero-screenshot.png</files>
  <action>
This is a human verification checkpoint. All Phase 1 foundation work is complete:
- SSE race condition fixed (subscribe before replay)
- Anthropic SDK replaces claude CLI
- TraceTimeline shows replanning events with amber styling
- Zustand store reloads DAG on task.replanned
- Docker Compose one-click startup
- Demo agents seeded automatically
- README rewritten with Docker-first quickstart

Steps to verify:
1. Start the platform: `ANTHROPIC_API_KEY=your-key docker compose up --build`
2. Open http://localhost:3000
3. Verify 4 demo agents visible on the Agents page
4. Create a task (e.g., "Evaluate whether we should adopt Kubernetes for our infrastructure")
5. Watch the DAG view — verify nodes transition smoothly from pending to running to completed (no frozen nodes)
6. Switch to Timeline tab — verify chronological events appear with agent names, durations, and badges
7. If a subtask fails and replanning occurs, verify the amber "Replanned" event appears in timeline
8. Capture a screenshot of the task detail page showing the DAG view or timeline for README hero image
9. Save screenshot to `docs/images/hero-screenshot.png` and update README if the path differs

Resume signal: Type "approved" with the screenshot path, or describe any issues found.
  </action>
  <verify>
    <automated>test -f docs/images/hero-screenshot.png && echo "Screenshot exists" || echo "Screenshot not yet captured"</automated>
  </verify>
  <done>Screenshot captured and saved. All Phase 1 features verified working end-to-end: SSE delivers all events, DAG nodes transition correctly, timeline shows replanning events, Docker Compose starts cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | README is a static document with no runtime trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | README quickstart | mitigate | Quickstart shows `ANTHROPIC_API_KEY=sk-your-key` as placeholder, never a real key |
</threat_model>

<verification>
1. README.md has Docker-first quickstart
2. README.md has badge row, architecture diagram, features
3. Screenshot captured and referenced in README (after checkpoint)
</verification>

<success_criteria>
- README is credible and professional on first GitHub visit
- Docker quickstart works in 3 steps: clone, docker compose up, open browser
- Hero screenshot shows a real working task (captured during checkpoint)
- Developer with just Docker + API key can go from zero to working demo
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
