"use client";

export function AgentTopology() {
  // V2 placeholder — will be replaced with DAG pipeline view
  return (
    <div className="flex items-center justify-center h-full text-gray-600 text-sm">
      Agent topology — coming in V2
    </div>
  );
}
