"use client";

export function MessageFeed() {
  // V2 placeholder — will be replaced with org-scoped channel messaging
  return (
    <div className="flex items-center justify-center h-full text-gray-600 text-sm">
      Channel messaging — coming in V2
    </div>
  );
}
