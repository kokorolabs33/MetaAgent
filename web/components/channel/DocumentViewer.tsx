"use client";

export function DocumentViewer() {
  // V2 placeholder — will be replaced with org-scoped document viewer
  return (
    <div className="flex items-center justify-center h-full text-gray-600 text-sm">
      Document viewer — coming in V2
    </div>
  );
}
